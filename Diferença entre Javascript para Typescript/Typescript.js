"use strict";

// Foi gerado um arquivo um javascript pelo arquivo typescript que foi compilado
function somar(numero1, numero2) {
    return numero1 + numero2;
}
let firstNumber = 3;
let secondNumber = 4;
const results = somar(firstNumber, secondNumber);
console.log(results);

